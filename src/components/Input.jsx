import React, { useState } from 'react'

const Input = () => {
  const { type, name, value } = this.this.props
  
  return (
    <input type={type} name={name} value={value} />
  )
}

export default Input